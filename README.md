# Initial Setup

This script helps in setting up the initial environment by checking for `git` and `gh`.

## Usage

You can run this script directly using the following command:

```bash
curl -sL https://raw.githubusercontent.com/riteshsingla95/public/main/initial_setup.sh | bash
```